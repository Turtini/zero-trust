% ================================
% Turtini PDF Style (Portable)
% Matches CLI Cheat Sheets
% ================================

% --- Core packages (portable) ---
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{geometry}
\usepackage{setspace}

% --- Page geometry ---
\geometry{margin=1in}

% --- Fonts (set by Pandoc, but safe defaults) ---
% Pandoc CLI sets:
% -V mainfont=Arimo
% -V monofont=Arimo

\renewcommand{\familydefault}{\sfdefault}

% --- Line spacing ---
\setstretch{1.1}

% --- Fancy header/footer everywhere ---
\pagestyle{fancy}
\fancyhf{}

\setlength{\headheight}{18pt}

% --- Header ---
\fancyhead[L]{%
  \includegraphics[height=14pt]{assets/logo.png}%
}
\fancyhead[C]{%
  \small\textcolor{gray}{Zero Trust Architecture — Operational Reference}%
}
\fancyhead[R]{%
  \small\textcolor{gray}{Page \thepage}%
}

% --- Footer ---
\fancyfoot[L]{%
  \small\textcolor{gray}{github.com/Turtini/zero-trust}%
}
\fancyfoot[R]{%
  \small\textcolor{gray}{\today}%
}

% --- Rules ---
\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\footrulewidth}{0pt}

% --- Force Pandoc title page to use fancy ---
\fancypagestyle{plain}{
  \pagestyle{fancy}
}

% --- Code blocks (Pandoc-safe, no Shaded env) ---
\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{
  fontsize=\small,
  breaklines=true,
  commandchars=\\\{\}
}

% --- Tables ---
\usepackage{longtable}
\usepackage{booktabs}

% --- Lists (portable defaults) ---
\usepackage{enumitem}
\setlist{nosep}

% ================================
% End Turtini PDF Style
% ================================

