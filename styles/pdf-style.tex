% styles/pdf-style.tex
% Turtini PDF style (portable) — header/footer branding + sane defaults

% --- Core packages ---
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{geometry}
\usepackage{sectsty}

% --- Page layout ---
\geometry{margin=1in}

% --- Section styling ---
\sectionfont{\bfseries}

% --- Fancy header/footer everywhere ---
\pagestyle{fancy}
\fancyhf{}

% Pandoc/XeLaTeX can be picky: must be tall enough for the header content
\setlength{\headheight}{18pt}

% Header (left: logo, center: doc label, right: page #)
\fancyhead[L]{%
  \includegraphics[height=14pt]{assets/logo.png}%
}
\fancyhead[C]{%
  \small\textcolor{gray}{Zero Trust Architecture — Operational Reference}%
}
\fancyhead[R]{%
  \small\textcolor{gray}{Page \thepage}%
}

% Footer (left: repo, right: date)
\fancyfoot[L]{%
  \small\textcolor{gray}{github.com/Turtini/zero-trust}%
}
\fancyfoot[R]{%
  \small\textcolor{gray}{\today}%
}

% Rules
\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\footrulewidth}{0pt}

% Force Pandoc title page ("plain" style) to use fancy
\fancypagestyle{plain}{%
  \pagestyle{fancy}%
}

