% ============================
% Turtini PDF Style (Portable)
% ============================

% --- Core packages ---
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{geometry}
\usepackage{sectsty}
\usepackage{setspace}

% --- Page geometry ---
\geometry{margin=1in}

% --- Fonts (portable on Ubuntu GitHub runners) ---
\usepackage{fontspec}
\setmainfont{Liberation Serif}
\setsansfont{Liberation Sans}
\setmonofont{Liberation Mono}

% --- Section styling ---
\sectionfont{\bfseries}
\subsectionfont{\bfseries}
\subsubsectionfont{\bfseries}

% --- Header / Footer ---
\pagestyle{fancy}
\fancyhf{}

\setlength{\headheight}{18pt}

% Header
\fancyhead[L]{%
  \includegraphics[height=14pt]{assets/logo.png}%
}
\fancyhead[C]{%
  \small\textcolor{gray}{Zero Trust Architecture — Operational Reference}%
}
\fancyhead[R]{%
  \small\textcolor{gray}{Page \thepage}%
}

% Footer
\fancyfoot[L]{%
  \small\textcolor{gray}{github.com/Turtini/zero-trust}%
}
\fancyfoot[R]{%
  \small\textcolor{gray}{\today}%
}

\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\footrulewidth}{0pt}

% --- Ensure Pandoc title page uses fancy header ---
\fancypagestyle{plain}{
  \pagestyle{fancy}
}

% --- Paragraph spacing (clean, report-like) ---
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% --- Line spacing ---
\onehalfspacing

